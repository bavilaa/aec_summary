{
  "name": "transcript-studio",
  "version": "1.0.0",
  "private": true,
  "description": "A focused browser and exporter for SRT transcripts.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
